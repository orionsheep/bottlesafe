模型：Qwen3-VL-4B-Instruct + 4-bit QLoRA
数据：300 条训练记录（272 train / 28 validation）
训练：1 epoch，34 optimizer steps
训练时长：1874 秒（约 31 分钟）
最终 train_loss：1.382
最终 eval_loss：1.065
状态：训练完成，最终适配器已保存并可重新加载

Windows 最终适配器：02_微调权重/public-300-final
Windows 可恢复检查点：02_微调权重/public-300-checkpoint-34
WSL 完整输出：/home/snjhz/chemical-safety-training/outputs/public-300
基础模型：/home/snjhz/chemical-safety-training/models/Qwen3-VL-4B-Instruct

验证结果：
1. 基础模型与正式 LoRA 适配器重新加载成功。
2. 商品图推理生成合法 JSON，并通过项目 schema 校验。
3. 一次 HAZMAT 图推理出现 JSON 标点格式错误，因此当前模型的 JSON 结构稳定性并非 100%；应用层应保留 JSON 修复或重试机制。

数据限制：300 条属于小规模微调，适合验证任务格式和初步能力，不等同于生产级安全识别模型。商品成分、浓度、急救和禁忌混用仍必须以清晰背标或 SDS 为依据。
